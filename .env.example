# Supabase Configuration
# For local development, use 127.0.0.1 instead of localhost to force IPv4 and avoid IPv6 DNS resolution issues
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=find-anon-key-from-supabase-startup
SUPABASE_JWT_SECRET=super-secret-jwt-token-for-ttanalysis-local-dev
# Add the service role key (needed for admin operations)
SUPABASE_SERVICE_ROLE_KEY=find-service-role-key-from-supabase-startup

# Admin Management (for CLI tool only)
# Set these for production to use the admin promotion CLI tool
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# PostgreSQL Configuration (Supabase)
# Use 127.0.0.1 instead of localhost to force IPv4 connection and prevent network hangs
VITE_PG_HOST=127.0.0.1
VITE_PG_PORT=54322
VITE_PG_DATABASE=postgres
VITE_PG_USER=postgres
VITE_PG_PASSWORD=postgres

# Development Settings
NODE_ENV=development